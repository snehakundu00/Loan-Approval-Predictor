# Loan-Approval-Predictor
A complete Machine Learning workflow for predicting whether a loan application will be approved (1) or rejected (0) based on applicant financial and demographic data. The project includes data preprocessing, model training, evaluation, and deployment as a Streamlit web app.
